# Инструкция по настройке PWA

## Проблема: добавляется только ярлык, а не приложение

Для полноценной установки PWA необходимо выполнить следующие шаги:

### 1. Создание иконок (ОБЯЗАТЕЛЬНО!)

Без иконок браузер не установит приложение как полноценное PWA.

**Способ 1: Использовать генератор иконок**
1. Откройте файл `create-icons-simple.html` в браузере
2. Нажмите кнопки "Скачать" для обеих иконок (192x192 и 512x512)
3. Сохраните файлы `icon-192.png` и `icon-512.png` в папку проекта

**Способ 2: Создать вручную**
- Создайте два PNG файла: `icon-192.png` (192x192) и `icon-512.png` (512x512)
- Используйте любой графический редактор
- Рекомендуется использовать иконку с клюшкой и шайбой (как в приложении)

### 2. Проверка требований PWA

Убедитесь, что выполнены все требования:

✅ **HTTPS или localhost**
- Приложение должно быть на HTTPS (или localhost для разработки)
- Для production используйте GitHub Pages или другой хостинг с HTTPS

✅ **Валидный manifest.json**
- Файл должен быть доступен по адресу `/manifest.json`
- Все поля должны быть заполнены правильно

✅ **Service Worker**
- Должен быть зарегистрирован и активен
- Проверьте в DevTools → Application → Service Workers

✅ **Иконки**
- Должны быть доступны по путям, указанным в manifest.json
- Минимальные размеры: 192x192 и 512x512

### 3. Проверка установки

1. **Откройте DevTools (F12)**
2. **Вкладка Application → Manifest**
   - Проверьте, что manifest загружен без ошибок
   - Убедитесь, что иконки найдены (зеленые галочки)

3. **Вкладка Application → Service Workers**
   - Должен быть зарегистрирован `service-worker.js`
   - Статус должен быть "activated and is running"

4. **Попробуйте установить:**
   - Нажмите кнопку "📱 Установить приложение" в приложении
   - Или используйте меню браузера (три точки → "Установить приложение")

### 4. Решение проблем

**Если иконки не загружаются:**
- Проверьте, что файлы `icon-192.png` и `icon-512.png` находятся в корне проекта
- Проверьте пути в `manifest.json`
- Очистите кэш браузера (Ctrl+Shift+Delete)

**Если Service Worker не регистрируется:**
- Проверьте консоль браузера на ошибки
- Убедитесь, что файл `service-worker.js` доступен
- Проверьте, что используется HTTPS или localhost

**Если установка не работает:**
- Убедитесь, что все требования выполнены
- Проверьте DevTools → Application → Manifest на ошибки
- Попробуйте в режиме инкогнито (чтобы исключить расширения)

### 5. Тестирование офлайн работы

После установки:
1. Отключите интернет (режим полета)
2. Откройте установленное приложение
3. Все функции должны работать, кроме экспорта JPEG (требует html2canvas)

## Быстрая проверка

Откройте консоль браузера (F12) и проверьте:
- ✅ `Service Worker зарегистрирован` - Service Worker работает
- ✅ `PWA готов к установке` - все настроено правильно
- ❌ Любые ошибки - нужно исправить

## Контакты для помощи

Если проблема не решена:
1. Проверьте консоль браузера на ошибки
2. Проверьте DevTools → Application → Manifest
3. Убедитесь, что все файлы на месте
